# 大規模コードベースでの Claude Code：動作の仕組み、ベストプラクティス、始め方

Claude Code は、数百万行規模のモノレポ、数十年続くレガシー、数十のリポジトリにまたがる分散アーキテクチャ、そして数千人規模の開発組織といった本番環境で稼働しています。こうした環境では、サブディレクトリごとに異なるビルドコマンドや、共通ルートのないフォルダに散らばったレガシーなど、小規模で単純なコードベースとは異なる課題が出ます。

本稿では、大規模展開で Claude Code の導入がうまくいった事例から観測されたパターンをまとめます。「大規模コードベース」には、数百万行のモノレポ、何十年もかけて積み上げたレガシー、別リポジトリに分かれた多数のマイクロサービス、あるいはそれらの組み合わせなど、幅広い形態を含めます。AI コーディングと縁遠いと思われがちな C、C++、C#、Java、PHP などの言語で動いているコードベースも対象です（最近のモデルでは、多くのチームが想定以上に Claude Code が活きるケースが増えています）。バージョン管理やチーム構成、蓄積された慣習によって展開の形はそれぞれ違いますが、ここに挙げるパターンは横断的に当てはまり、Claude Code の導入を検討するチームの出発点になります。

## Claude Code は大規模コードベースをどう辿るか

Claude Code は、ソフトウェアエンジニアと同じようにコードベースを扱います。ファイルシステムをたどり、ファイルを読み、grep で必要な箇所を特定し、参照をコードベース全体に追います。開発者のマシン上でローカルに動作し、インデックスの構築・保守・アップロードは不要です。

RAG 型の AI コーディングツールは、コードベース全体を埋め込み、クエリ時に関連チャンクを取り出します。大規模になると、活発にコミットが続くチームに埋め込みパイプラインが追いつかず破綻しがちです。開発者がインデックスを参照する時点では、すでに数週間・数日・場合によっては数時間前のコードベースの写像になっていることがあります。取り出し結果は、2 週間前にリネーム済みの関数を指したり、直近のスプリントで削除されたモジュールを参照したりし、古い情報だという表示は出ません。

エージェント的な検索はそうした失敗モードを避けます。数千人がコミットしても、埋め込みパイプラインや集中インデックスを常にメンテする必要はありません。各開発者のインスタンスは、常にその場のコードベースを前提に動きます。

一方でトレードオフがあります。Claude が「どこを見るか」の出発点となる文脈が十分にあるときに最も効きます。つまりナビゲーションの質は、CLAUDE.md や skills などでコードベースがどれだけ整備されているかに左右されます。10 億行規模で曖昧なパターンの全出現を頼むと、作業に入る前にコンテキストウィンドウの限界に達します。セットアップに投資したチームほど結果が良くなります。

## モデルと同じくらい「ハーネス」が効く

Claude Code についてよくある誤解は、「能力は使うモデルだけで決まる」というものです。チームはベンチマークやテストタスクの成績に注目しがちですが、実務ではモデルの周りに構築されたエコシステム——**ハーネス**——の方が、モデル単体よりパフォーマンスを決めます。

ハーネスは 5 つの拡張点から成ります。CLAUDE.md、hooks、skills、plugins、MCP サーバーで、それぞれ役割が違います。導入順も重要で、各層は前の層の上に積み上がります。加えて **LSP 連携**と**サブエージェント**がセットアップを補完します。以下、各コンポーネントと能力の説明です。

[**CLAUDE.md**](https://code.claude.com/docs/en/memory) **が最初。** セッション開始時に Claude が自動で読むコンテキストファイルです。ルートで全体像、サブディレクトリでローカル慣習を書きます。どんな作業をうまくするにも必要な「コードベースの知識」を与えます。毎セッション必ず読み込まれるため、広く効くことだけに絞らないとパフォーマンスの足を引っ張ります。

[**Hooks**](https://code.claude.com/docs/en/hooks-guide) **でセットアップを自己改善させる。** 多くのチームは「Claude の誤動作を防ぐスクリプト」と捉えがちですが、より価値があるのは**継続的改善**です。stop hook はセッションを振り返り、文脈が新しいうちに CLAUDE.md 更新を提案できます。start hook はチーム固有のコンテキストを動的に読み込み、各開発者が手動設定せずにモジュールに合った設定を得られます。Lint やフォーマットなどの自動チェックは、hook でルールを決定的に適用し、Claude に指示を覚えさせるより一貫した結果になります。

[**Skills**](https://code.claude.com/docs/en/skills) **で、毎セッションを膨らませずに必要な専門性だけをオンデマンドで。** 大規模コードベースではタスク種別が多く、すべての専門知識を毎回載せる必要はありません。Skills は [progressive disclosure](https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices) で、本来コンテキストを奪い合う専門ワークフローやドメイン知識をオフロードし、タスクが必要とするときだけ読み込みます。例：脆弱性評価ならセキュリティレビュー skill、コード変更に伴う文書更新ならドキュメント処理 skill。

Skills はパス単位でスコープできます。決済サービスを持つチームは、そのディレクトリにだけデプロイ skill を紐づけ、モノレポの別パートで作業しているときには自動ロードされないようにできます。

[**Plugins**](https://code.claude.com/docs/en/plugins) **で「効く構成」を配る。** 大規模コードベースでは、**良い**セットアップがチーム内限りに閉じやすい問題があります。Plugin は skills・hooks・MCP 設定を 1 つのインストール可能パッケージにまとめ、新入社員が入社初日に入れるだけで、既に Claude を使っている人と同じコンテキストと能力をすぐ持てます。更新は [managed marketplaces](https://support.claude.com/en/articles/13837433-manage-claude-cowork-plugins-for-your-organization) で組織に配布できます。

例：協業している大規模小売企業は、社内分析基盤へ Claude をつなぐ skill を作り、ビジネスアナリストがワークフローを離れずにパフォーマンスデータを引けるようにしました。全社展開前に plugin として配布した、という流れです。

**LSP（Language Server Protocol）連携で、IDE と同じナビゲーションを Claude に。** 大規模向け IDE には多くの場合 LSP が動き、「定義へジャンプ」「参照のすべて」を支えています。これを Claude に見せると**シンボル単位**の精度になり、関数呼び出しから定義へ辿れ、ファイル横断で参照を追え、異なる言語で同名の関数があっても区別できます。なければテキストの類似に頼り、誤ったシンボルに着地することがあります。協業したあるエンタープライズソフトウェア企業は、Claude Code 展開の前に組織全体で LSP 連携を入れ、特に C/C++ の大規模ナビゲーションを信頼できるようにしました。多言語コードベースでは、投資対効果の高い一手のひとつです。

**MCP サーバーがすべてを拡張。** 社内ツール、データソース、API など、通常は届かない先へ Claude をつなぐのが MCP です。高度なチームは、構造化検索をツールとして Claude に直接呼ばせる MCP を用意しています。社内ドキュメント、チケット、分析基盤などにつなぐ例もあります。

[**Subagents**](https://code.claude.com/docs/en/sub-agents) **で探索と編集を分ける。** サブエージェントは独立した Claude インスタンスで、独自のコンテキストウィンドウを持ち、タスクを受け取り、**親には最終結果だけ**返します。ハーネスが整ったあとは、読み取り専用サブエージェントでサブシステムをマップし所見をファイルに書き、メインエージェントが全体像を踏まえて編集する、という形を取るチームもあります。

![](https://cdn.prod.website-files.com/68a44d4040f98a4adf2207b6/6a04aaf1c37c6196e5ee19bb_fig1-the-claude-code-harness-v1%402x.png)

Claude Code の拡張層の概要。

下表は各コンポーネントの役割、いつ読み込まれるか、よくある誤解の整理です。

<table><thead><tr><th>コンポーネント</th><th>何か</th><th>読み込みタイミング</th><th>向いている用途</th><th>よくある混乱</th></tr></thead><tbody><tr><td>CLAUDE.md</td><td>Claude が自動で読むコンテキストファイル</td><td>毎セッション</td><td>プロジェクト固有の慣習、コードベースの知識</td><td>skill に置くべき再利用できる専門知識を全部ここに書く</td></tr><tr><td>Hooks</td><td>重要なタイミングで動くスクリプト</td><td>イベントで発火</td><td>一貫した挙動の自動化、セッションから学びの取り込み</td><td>自動で走らせるべきものをプロンプト任せにする</td></tr><tr><td>Skills</td><td>特定タスク向けにパッケージされた指示</td><td>関連するときオンデマンド</td><td>セッションやプロジェクトをまたぐ再利用の専門性</td><td>すべて CLAUDE.md に載せる</td></tr><tr><td>Plugins</td><td>skills・hooks・MCP 設定の束</td><td>設定後は常に利用可能</td><td>組織全体に「動くセットアップ」を配る</td><td>良い構成をチーム内秘にしたままにする</td></tr><tr><td>LSP（Language Server Protocol）*</td><td>言語サーバー経由のリアルタイムなコードインテリジェンス</td><td>設定後は常に利用可能</td><td>シンボル単位のナビゲーション、型付き言語でのエラー検知</td><td>自動で有効だと思い込む</td></tr><tr><td>MCP サーバー</td><td>外部ツール・データへの接続</td><td>設定後は常に利用可能</td><td>通常は届かない社内ツールへのアクセス</td><td>基礎が固まる前に MCP から手を出す</td></tr><tr><td>Subagents*</td><td>特定タスク用の別 Claude インスタンス</td><td>呼び出したとき</td><td>探索と編集の分離、並列作業</td><td>同一セッションで探索と編集を混在させる</td></tr><tr><td colspan="5">*LSP は plugin 層経由で使う。Subagents は設定する拡張点というより**委譲**の能力。</td></tr></tbody></table>

## 成功した展開から見える 3 つの構成パターン

大規模コードベース向けの Claude Code の構成は、リポジトリの構造に強く依存します。それでも観測した展開では、次の 3 つのパターンが繰り返し現れました。

### スケールで「辿れる」コードベースにする

大規模コードベースで Claude が役立つかは、**適切な文脈を見つけられるか**で決まります。毎セッションに載せすぎると性能が落ち、載せなさすぎると手探りになります。効果的な展開では、事前投資で Claude にとって読みやすいコードベースにします。繰り返し見えるパターンは次のとおりです。

- **CLAUDE.md は薄くレイヤー化。** Claude はディレクトリを移動するにつれて加算的に読みます。ルートは全体像、サブはローカル慣習。ルートはポインタと致命的な落とし穴だけに。それ以外はノイズになりやすいです。
- **リポジトリルートではなくサブディレクトリで初期化。** タスクに実際に関係する範囲にスコープがあるとき最も効きます。モノレポではツールがルート前提になりがちで違和感がありますが、Claude はディレクトリを上に辿り、見つかった CLAUDE.md をすべて読むため、ルートの文脈が失われるわけではありません。
- **テスト・lint コマンドをサブディレクトリごとにスコープ。** 1 サービスを変えただけでフルスイートを走らせるとタイムアウトし、無関係な出力でコンテキストを浪費します。サブディレクトリの CLAUDE.md には、その部分に効くコマンドを書く。サービス指向でディレクトリごとにテスト・ビルドが分かれている構成と相性が良いです。コンパイル言語のモノレポでディレクトリ間依存が深い場合は、サブごとのスコープは難しく、プロジェクト固有のビルド設定が要ることもあります。
- **`.`**`ignore` **で生成物、ビルド成果物、サードパーティのコードを除外。** `.claude/settings.json` に `permissions.deny` ルールをコミットすれば除外もバージョン管理され、全員が同じノイズ削減を個別設定なしで得られます。一部のコードベースでは生成物そのものが開発対象。ジェネレータ担当者はローカル設定でプロジェクトレベルの除外を上書きでき、他のメンバーには影響しません。
- **ディレクトリ構造だけでは足りないときはコードベースマップ。** コードが通常のディレクトリ構造にまとまっていない組織では、ルートに軽量な Markdown で各トップレベルフォルダと一行説明を並べ、Claude がファイルを開く前に読める目次にします。トップレベルが数百ある場合はレイヤー化が効く：ルートは最上位の構造だけ、詳細はサブの CLAUDE.md が Claude が木を降りるにつれオンデマンドで読み込む。単純なら、参照してほしいファイルやディレクトリを @ で明示するだけで同じ役割になります。
- **LSP を動かし、文字列ではなくシンボルで検索。** 大規模で一般的な関数名を grep すると数千ヒットがあり、どれが本物か開きまくってコンテキストを消費します。LSP は同一シンボルを指す参照だけ返し、**Claude が読む前に**絞り込めます。セットアップには、言語向けの [code intelligence plugin](https://code.claude.com/docs/en/discover-plugins#code-intelligence) と対応する language server バイナリの導入が必要です。利用可能な plugin とトラブルシュートは Claude Code のドキュメントを参照してください。

**注意**：階層的 CLAUDE.md でも破綻する例があります。数十万フォルダ・数百万ファイル、Git 以外のバージョン管理のレガシーなどです。そうした課題は本シリーズの続編で扱う予定です。

### モデルの知性の進化に合わせて CLAUDE.md を積極的にメンテする

モデルが進むと、今のモデル向けに書いた指示が将来のモデルでは邪魔になることがあります。かつて苦手だったパターンを導く CLAUDE.md は、次のモデルでは不要になるか、むしろ拘束になることがあります。例：リファクタを常に単一ファイルの変更に分解させるルールは、古いモデルの軌道修正には効いても、新しいモデルが得意な複数ファイルにまたがる協調した編集を妨げます。

特定モデルや Claude Code ツールの限界を補うために作った skills や hooks は、限界が解消されると**オーバーヘッド**になります。例：Perforce コードベースで p4 edit を強制するためにファイル書き込みを傍受した hook は、Claude Code にネイティブの Perforce モードが入ってからは冗長になりました。

**3〜6 ヶ月ごと**にまとまった設定レビューを想定しつつ、大きなモデルアップデートのあと性能が頭打ちに感じたら、そのタイミングでも見直す価値があります。

### Claude Code の管理と普及にオーナーを置く

技術設定だけでは普及しません。うまくいった組織は**組織側**にも投資しました。

最も速く広がった展開は、全社展開の前にインフラに少し手を入れていたケースです。小さなチーム、時には 1 人だけが、開発者が最初に触れたときからワークフローに馴染むよう配線しました。ある企業では数名のエンジニアが plugins と MCP の一式を初日から使えるようにしました。別の企業では AI コーディングツール専門チームが、展開前にインフラを整えていました。いずれも最初の体験が「イライラ」ではなく「できる」だったため、そこから普及が広がりました。

![](https://cdn.prod.website-files.com/68a44d4040f98a4adf2207b6/6a04e25f1984beb50dc5525b_fig2-phases-of-claude-code-rollout-v1%402x.png)

今日、こうした仕事を担うチームは多くの場合 Developer Experience（開発者体験）や Developer Productivity に置かれ、新入社員のオンボーディングや開発者向けツールを担う機能です。複数組織で新しく見え始めているのが **agent manager**：Claude Code のエコシステム管理に振り切った PM とエンジニアのハイブリッド役割です。専任チームが難しい場合の最小形は **DRI**：Claude Code の設定、設定・権限ポリシー、plugin マーケットプレイス、CLAUDE.md の慣習について決定権と更新責任を持つ 1 人です。

ボトムアップの普及は熱量を生みますが、**何が効くかを束ねる人**がいないと分断します。標準化した CLAUDE.md の階層や、キュレーションした skills・plugins といった慣習を組み立て、広める個人またはチームが必要です。そうしないと知識は部族知のまま、普及は頭打ちになります。

大企業、とくに規制産業では、**ガバナンス**が早い段階で出ます。例：誰がどの skills / plugins を配る権限を持つか、数千人が同じものをバラバラに再実装しないようにするには、AI が書いたコードも人が書いたのと同じレビューを通すには。早期対処として、承認済み skills の明文化、必須のコードレビュー、初期アクセスの制限から始め、信頼が積み上がるにつれ広げることを勧めます。

エンジニアリング・情報セキュリティ・ガバナンスの代表を早い段階で集めた**横断ワーキンググループ**を立ち上げ、要件とロードマップを一緒に定義した組織で、最もスムーズな展開を観測しています。

## 自組織への適用

Claude Code は、**エンジニアが主なコントリビュータ**で、**Git** を使い、**標準的なディレクトリ構造**に沿う、といった従来型のソフトウェア開発環境を前提に設計されています。大規模コードベースの多くはこの型に収まりますが、巨大バイナリ資産のあるゲームエンジン、非従来型のバージョン管理、エンジニア以外がコードに触る環境などは**追加の構成作業**が要ります。本稿の前提は従来型のセットアップであり、述べたパターンは多くの顧客で機能してきました。残る複雑さは、コードベース・ツール・組織ごとの判断が必要で、そこでは Anthropic の Applied AI チームがエンジニアリングチームと直接組み、これらのパターンを貴社の要件へ落とし込む支援をしています。

![](https://cdn.prod.website-files.com/68a44d4040f98a4adf2207b6/6a04e2860abbe67418ca0f8b_fig3-getting-started-checklist-v2%402x.png)

[*Claude Code for Enterprise*](https://claude.com/product/claude-code/enterprise) *から始める。*

***謝辞：*** *大規模展開の経験を共有してくださった Anthropic Applied AI チームの Alon Krifcher、Charmaine Lee、Chris Concannon、Harsh Patel、Henrique Savelli、Jason Schwartz、Jonah Dueck、Kirby Kohlmorgen に深く感謝します。本稿へのフィードバックをいただいた Zoox の Amit Navindgi にも感謝します。*
